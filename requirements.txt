# Core chess functionality
python-chess[engine]>=1.999

# Rich terminal UI
rich>=13.0.0

# LLM Providers (optional - install as needed)
openai>=1.0.0
anthropic>=0.3.0
google-generativeai>=0.3.0
python-dotenv>=0.19.0

# Human-like chess engines (optional - engines must be installed separately)
# These are utility packages that may help with engine management
requests>=2.25.0  # For downloading engine binaries if needed
psutil>=5.8.0     # For better process management of engines

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Type checking (optional)
mypy>=1.0.0


# Linting and formatting (optional)
black>=23.0.0
flake8>=6.0.0
isort>=5.0.0
